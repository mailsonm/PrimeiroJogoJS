class TelaFinal {
    draw() {
      background(0);
      textSize(60);
      fill(255);
      textAlign(CENTER);
      text('Game Over', width / 2, height / 2);
      textSize(30);
      text('Pressione qualquer tecla para reiniciar', width / 2, height / 2 + 60);
    }
  }
  